# 待修改（Secure SDLC 計畫）— V4 功能重設計

> CoreMain.md 已於 2026-06-06 建立。
> V4 核心方向：以「任務為核心」重新整理所有功能，簡單、清楚、順手。
> 移除與主題距離遠的功能，重新設計資訊架構。

---

## 目標

依 CoreMain.md 的「任務核心」主題，對 WorkFlow 進行功能重設計：
1. 移除偏離主題的功能（Webhook、公開分享、多語言）
2. 簡化過度複雜的功能（儀表板、視圖、工時報表）
3. 升級核心功能（AI 建議接 Claude API）
4. 重新設計「個人首頁」讓使用者 5 秒找到今日下一步

---

## 範圍與假設

- **範圍**：功能重設計，含後端 API 調整 + 前端 UX 重構；不做新 DB migration（除 AI 升級）
- **明確不做**：F18 Webhook、F21 i18n、F22 公開分享（已決定移除）
- **暫緩**：F08 週報、F14 Emoji 反應、F16 個人效率分析
- **技術棧**：同 V3（FastAPI + PostgreSQL + Redis + React/Vite + TailwindCSS + Zustand）
- **部署環境**：本機 Docker Compose（V3 已通過 E2E 20/20）
- **AI 升級前提**：Claude API Key 需使用者另行提供

---

## 上線標準（Exit Criteria，三維度合一）

- **功能**：N01～N09 重設計功能各自的驗收項目通過；核心 UX 路徑（查任務→改狀態→評論）≤ 3 次點擊
- **資安**：Critical/High = 0；移除功能不留殘餘 API endpoint；AI 功能 API Key 走環境變數
- **品質**：E2E Smoke Test Pass 20/20；Critical/Major 缺陷 = 0

---

## 〔PM〕V4 功能設計

### CoreMain 對照結論

| 類別 | 功能 | 決定 | V4 設計方向 |
|------|------|------|-------------|
| ✅ 保留 | F02 子任務 | 保留不變 | 維持現有設計 |
| ✅ 保留 | F03 @Mention 通知 | 保留不變 | 維持現有設計 |
| ✅ 保留 | F04 搜尋與篩選 | 保留不變 | 維持現有設計 |
| ✅ 保留 | F06 任務依賴關係 | 保留不變 | 維持現有設計 |
| ✅ 保留 | F10 重複排程任務 | 保留不變 | 維持現有設計 |
| ✅ 保留 | F11 檔案附件 | 保留不變 | 維持現有設計 |
| ✅ 保留 | F13 批次操作 | 保留不變 | 維持現有設計 |
| ✅ 保留 | F15 任務 Check-in | 保留不變 | 維持現有設計 |
| ✅ 保留 | F19 暗色模式 | 保留不變 | 維持現有設計 |
| 🔧 重設計 | F01 儀表板 → N01 | 個人首頁重設計 | 見下方 N01 |
| 🔧 重設計 | F09 視圖切換 → N02 | 移除試算表視圖 | 見下方 N02 |
| 🔧 重設計 | F07 時間追蹤 → N03 | 移除獨立報表頁 | 見下方 N03 |
| 🔧 重設計 | F05 自訂欄位 → N04 | 限 5 欄，簡化 UX | 見下方 N04 |
| 🔧 重設計 | F12 工作量視圖 → N05 | 整合進儀表板 | 見下方 N05 |
| 🔧 重設計 | F17 公告板 → N06 | 首頁橫幅取代獨立頁 | 見下方 N06 |
| 🔧 重設計 | F23 健康指標 → N07 | 改為 3 級文字標籤 | 見下方 N07 |
| 🔧 升級 | F24 AI 建議 → N08 | 接 Claude API | 見下方 N08 |
| 🔧 簡化 | F20 匯出 → N09 | 只保留任務 CSV | 見下方 N09 |
| ❌ 移除 | F18 Webhook | 移除後端 + 前端 | 清理 API + UI |
| ❌ 移除 | F22 公開分享 | 移除後端 + 前端 | 清理 API + UI |
| ⏸ 暫緩 | F08/F14/F16 | 不在 V4 範圍 | 不動現有 code |

---
## 〔待實作〕後續功能調整

### G01 — 甘特圖依賴線不重疊 ✅ 已完成（2026-06-07）

**驗收**：
- [x] 多條依賴線存在時，折線垂直段不重疊（分配通道偏移 5px/條）
- [x] 線段仍可清楚辨識起點與終點任務

---

### G02 — 里程碑移除手動工時，改由日常任務加總 ✅ 已完成（2026-06-07）

**驗收**：
- [x] 完成記錄卡片上不再顯示工時輸入框和儲存按鈕
- [x] 統計卡「總工時」正確反映關聯日常任務時數加總
- [x] 備註欄位仍可填寫與儲存（onBlur 自動儲存）

---

### G03 — 封存功能重設計 ✅ 已完成（2026-06-08）

**背景**：降低日常作業與專案任務的資料載入壓力，並可追蹤歷史日常任務與專案歷史紀錄。

**實作內容**：

後端：
- [x] Migration 016：`users.auto_archive_days` 欄位（Integer, default=0）
- [x] User model / schema 加入 `auto_archive_days`（PATCH /users/me 支援更新）
- [x] `GET /daily-tasks/archive/history`：查詢歷史封存記錄（date_from/date_to/linked_task_id 篩選 + 工時統計）
- [x] `GET /daily-tasks/archive/history/export`：匯出 CSV（utf-8-sig，含工時欄）
- [x] `run_auto_archive()` 輔助函式：遍歷所有 auto_archive_days>0 的使用者，執行封存
- [x] `main.py` lifespan 加入自動排程 loop（每日 00:05 執行，CancelledError 優雅退出）

前端：
- [x] `types/index.ts`：User 加 `auto_archive_days`；新增 `ArchiveHistoryItem`, `ArchiveHistoryStats`, `ArchiveHistoryResponse`
- [x] `api/dailyTasks.ts`：加入 `getArchiveHistory()`, `exportArchiveHistoryCsv()`
- [x] `api/users.ts`：新增（`updateMe()` 支援 auto_archive_days）
- [x] `pages/HistoryPage.tsx`：新頁面，兩 Tab（歷史日常任務 + 封存專案）
- [x] `pages/ArchivedProjectsPage.tsx`：重構為「日常任務整理」頁（手動整理 + 自動整理設定）
- [x] `App.tsx`：新增 `/history` 路由
- [x] `Layout.tsx`：導航改為「任務整理 📦」+「歷史記錄 🗄️」兩項

**驗收**：
- [x] Migration 016 執行成功（alembic current = 016 head）
- [x] `GET /daily-tasks/archive/history` 回傳正確格式（空資料測試通過）
- [x] `PATCH /users/me { auto_archive_days: 90 }` 回傳更新後值
- [x] 前端 build 無錯誤，Vite 啟動成功
- [x] 後端 log 顯示 `auto_archive_days` 欄位正確載入

---

### G04 — 後端測試覆蓋率提升至 90% ✅ 已完成（2026-06-09）

**目標**：後端 pytest 覆蓋率 → 90%。CI gate 同步調升。**結果：92.14%，243 passed，CI gate 已調至 90。**

**關鍵發現（2026-06-09 收尾）**：先前報告的「67%」是**量測缺陷導致的低估**。
async endpoint body 在 anyio/greenlet task 內執行，coverage 預設 `concurrency=thread`
量不到這些行，導致大量成功路徑被誤判為未覆蓋。於 `pyproject.toml` 加入
`[tool.coverage.run] concurrency = ["greenlet", "thread"]` 後，真實覆蓋率即為 91%。
（同 prompt-monitor 專案 commit 6aa925b 的同類修復。）

**最終快照（2026-06-09）**：
- **覆蓋率：97.32%**（92% → 補 A 類 endpoint edge 分支 + websocket/manager 真測 → 97%）
- **測試數：310 passed**（起點 11 → 196 → 243 → 310）
- **CI gate：已調至 95%**（ci.yml `--cov-fail-under=95`，留 2% 餘裕；本地模擬 CI 通過 97.32%）
- **第二輪補測（92%→97%）**：daily_tasks archive/run_auto_archive、time_logs、attachments、
  milestones、daily_task_import(_parse_row+Excel)、websocket manager(fake WS)+ endpoint
  連線路徑、deps token 邊界、config 生產驗證、auth refresh、跨模組 403/404 gap-fill。
- **刻意不測（# pragma: no cover）**：main lifespan/auto_archive 無窮迴圈、crypto import-time
  金鑰解析、reactions 並發 IntegrityError、recurring 歷史 str due_date 相容。剩餘 ~86 行
  未覆蓋多為各模組 1-2 行的 ldap/db.session 啟動分支，邊際效益低，不硬推 100%。
- 本地驗證環境：`workflow-backend-1` 容器 + `workflow_test` DB
  指令：`docker exec -e DATABASE_URL="postgresql+asyncpg://workflow:workflow_local_pass_2026@db:5432/workflow_test" workflow-backend-1 sh -c "cd /app && python -m pytest tests/ --cov=app --cov-report=term-missing"`

**收尾批次（2026-06-09）**：
- [x] 刪除 dead code：`schemas/milestone.py`（無引用）、`projects.require_project_role`（無呼叫）
- [x] `pyproject.toml` 加 `[tool.coverage.run]` greenlet concurrency + `source=["app"]`
- [x] `test_projects_perm.py`：非成員 403 / 非 admin 過濾 / 角色不足擋關（14 測試，projects.py → 100%）
- [x] `test_templates_deep.py`：apply 依賴接線+工作日排程 / from-project / 權限（10 測試，→ 99%）
- [x] `test_tasks_deep.py`：update 通知+assignee 重設 / @mention / 角色權限（8 測試，→ 100%）
- [x] `test_ai_claude.py`：mock anthropic，測 `_get_claude_suggestions` 成功/解析/fallback（6 測試）
- [x] `test_system_settings_deep.py`：secret 加密遮罩 / test-ldap/radius 成功失敗分支（9 測試）

**已完成批次（每批本地實跑驗證 + ruff format/check + commit）**：
- [x] Batch 1：projects / tasks / subtasks CRUD
- [x] Batch 2：templates / milestones / daily_task_import
- [x] Batch 3：notifications / checkins / reactions
- [x] Batch 4：time_logs / custom_fields / dependencies / bulk_tasks
- [x] Batch 5：calendar / workload / insights / weekly / announcements / recurring / settings / users
- [x] Batch 6：daily_tasks deep（archive history/CSV/by-task）+ projects archive-sync
- [x] Batch 7：auth / crypto / ai_assist / attachments / dashboard
- [x] Batch 8：跨模組 gap-fill 14 測試
- [x] Batch 9a/b/c：security + ldap/radius(mock) + auth remote + dashboard 富資料 + main.py

**測試過程中發現並修復的 9 個真實 production bug**（皆已 commit）：
1. `tasks.update_task`：`expire_all()` 後存取 `current_user` 屬性 → MissingGreenlet，任務進度更新必 500
2. `api/v1/__init__`：`bulk_tasks.router` 註冊在 `tasks.router` 之後，`/tasks/bulk` 被 `/tasks/{task_id}` 攔截 → 批次操作全壞（422）
3. `announcements`：`expires_at is None`（Python identity）→ 無到期日公告永不顯示。改 `.is_(None)`
4. `recurring.spawn`：`due_date=next_due.isoformat()`（str 塞 Date 欄）→ asyncpg DataError，spawn 全壞
5. `weekly_reports`：`Task.due_date < now.date().isoformat()`（str 比 Date）→ overdue 篩選錯誤
6. `projects.update_project`：`values(due_date=str(end_date))` → 改專案 end_date 必 500
7. `projects.apply_deadline`：同上 `str(project.end_date)` Date bug
8. `daily_tasks.update`：`expire_on_commit=False` 下 label 更新不反映（session 快取）。改 `expire(task, ["labels"])`
9. `ai_assist`：`date.fromisoformat(task.due_date)`（Date 物件當 str）→ 有 due_date 任務開 AI 建議必 500

**下次接續點（剩 67% → 90%，約 750 行）**：
仍低覆蓋的模組（含未覆蓋行號，見 term-missing 報告）：
- `dashboard.py` 32%、`projects.py` 41%、`templates.py` 42%、`tasks.py` 45%、`recurring.py` 46%、`auth.py` 49%
- `schemas/milestone.py` 0%（疑 dead code，需確認是否刪除或加 schema 測試）
- `daily_tasks.py` 55%（export CSV 內容、archive scheduler 分支）、`notifications.py` 56%、`bulk_tasks.py` 57%
- 策略：逐模組讀 term-missing 行號 → 補 error/admin/edge 分支；admin-only 端點需確認 admin_user fixture 真正 promote
- 達 90% 後把 ci.yml gate 從 65 調至 90，pyproject 可加 `[tool.coverage]` 設定

**教訓**：寫 API 測試前先讀 endpoint return 與 model 欄位型別（多個 bug 源於 Date 欄位被當 str）；用本地 Docker test DB 實跑，勿靠推送 CI 當測試環境。

---

### G05 — 認證重構：username 登入 + remote-first fallback local + 來源互斥（進行中，2026-06-09）

> 走 Secure SDLC 編排（Gate G1~G6）。計畫：`C:\Users\zzx78\.claude\plans\swift-juggling-otter.md`（已核准）。
> CoreMain 對齊：服務「組織內部 100 人協作」的登入體驗與 SSO，屬基礎建設修正，圍繞主題、不過度設計。

**問題（為什麼做）**：
1. email 同時當登入鍵與聯絡信箱 → 無法改 email；remote 帳號 email 被綁死。
2. auth_backend 單選無 fallback → 純本地帳號在 ldap/radius 模式登不進；admin-always-local hack 導致 remote 帳號升 admin 後鎖死。
3. local 與 remote 帳號可隱性重疊，auto-provision 無防呆。

**上線標準（Exit Criteria 三維度）**：
- 功能：username 登入；remote-first→fallback local；local 可改 email、remote 自動帶遠端 email；前端登入/註冊/設定改 username。
- 資安：無認證繞過；來源互斥（local/remote 同名擋下）；remote 帳號不得升 admin（防鎖死）；無硬編碼密鑰；auth_source 不可被使用者竄改。
- 品質：後端覆蓋率 ≥95%（CI gate 不退）；既有測試全綠；新分支全覆蓋。

#### 〔PM〕評估與設計（G1）
- [x] 與使用者確認認證順序（remote-first fallback local）、互斥策略、欄位設計（username+auth_source）
- [x] 確認 admin 改走 remote-first、remote 不得升 admin（取代 always-local 逃生門）
- [x] 計畫核准（swift-juggling-otter.md）

#### 〔DevSecOps〕編寫與資安確認（G2/G3）
- [x] User model 加 username(unique)/auth_source；email 改 nullable 非唯一
- [x] Migration 017：加欄位 + 回填 username（email 前綴去重）+ email 去 unique
- [x] schemas/user.py：UserCreate/Update/Out/LoginRequest 改 username
- [x] auth.py：register 互斥檢查 + login remote-first fallback local（移除 admin-always-local）
- [x] users.py：update_me 限 local 改 email（update_user_role 原擋 remote 升 admin，2026-06-09 依需求開放 remote 可升 admin）
- [x] main.py：_ensure_superadmin 補 username/auth_source
- [x] 前端：types/api/authStore/Login/Register/Settings 改 username（build 通過）

##### 〔Sec〕資安確認結果（對照 OWASP A07 認證失效）
- ✅ **認證繞過**：login 兩段式（remote-first → fallback local），任一段失敗皆 raise 401；無預設放行路徑。
- ✅ **來源互斥**：username 全域唯一；remote 不接管 local 同名帳號（`user.auth_source==backend` 才走 remote）。register 撞任何來源同名 → 400。
- ✅ **提權**：role 變更受 require_admin 保護，無繞過。**remote 帳號可升 admin**（2026-06-09 依使用者需求開放）——因 login 不再強制 admin 走 local，remote admin 走目錄驗證不會鎖死；測試驗證升級後仍能登入。取捨：未強制保留 local admin，所有 admin 皆 remote 且目錄中斷時期間無 admin 可登入（已確認接受）。
- ✅ **密鑰**：無硬編碼；LDAP/RADIUS 密鑰走 system_settings 加密儲存（crypto.encrypt_secret）。
- ✅ **欄位防竄改**：auth_source 不在 UserUpdate schema → 使用者無法透過 PATCH /users/me 竄改來源（測試 test_user_cannot_tamper_auth_source 驗證）。
- ✅ **email 帶入**：remote 登入自動帶/更新目錄 email；RADIUS 無 email 屬預期（None）。
- **G3 過程發現並修正（真實設計缺陷）**：原 login 對「remote 成功但來源不符」直接 raise 401，導致 local 帳號在 ldap 模式下被同名 remote **鎖死**（連本地密碼都用不了）。改為「來源不符 → remote 不接管 → 落 fallback local」，互斥達成且不鎖死。見 test_source_conflict_local_user_cannot_login_via_remote。

#### 〔QA〕品質測試（G4）
- [x] conftest + 既有認證測試改 username 登入（既有 10 個 auth 測試破口全修，其餘 300 透過 fixture 自動適配）
- [x] test_auth_refactor.py（13 測試）：remote-first/fallback/互斥/auto-provision/email帶入/升admin擋/改email擋/admin fallback/auth_source防竄改/radius
- [x] 全套 pytest 97%（322→335 passed）、ruff All checks passed、前端 build 通過

#### 關卡狀態
- [x] G1 PM→Dev：設計與 Exit Criteria 確認
- [x] G2 Dev→Sec：功能完成、後端可建置、前端 build 通過、自測通過
- [x] G3 Sec→QA：資安 Exit Criteria 達標（OWASP A07 逐項；發現並修正互斥鎖死缺陷）
- [x] G4 QA→結案：品質 Exit Criteria 達標（覆蓋率 97% ≥95%、auth 全綠）
- [x] G5 結案→CI：CI 綠燈（run d897141，7/7 job success：Frontend/Backend Lint/SCA/Secret Scan/Backend Tests/SAST/Docker Build）
- [ ] G6 CI→CD：未觸及（無 DB 上線變更；migration 017 上線需另行確認）

##### G5 兩次退回紀錄（CI 擋下、回根因修）
1. **gitleaks 誤判 + ruff format**（commit 43e96c3）：placeholder 密碼 `NewStrong123` 進 allowlist、測試檔 path 改 regex；補跑 format。見教訓 60。
2. **attachment upload 環境不可寫**（commit d897141）：`UPLOAD_DIR=/app/uploads` 在 CI runner 不可寫 → conftest autouse fixture 重導 tmp_path。見教訓 61。
> 兩次皆回根因修（Dev/QA），未調寬 CI 門檻。

---

### G06 — 後端完整資訊安全檢視（2026-06-09）

> DevSecOps Sec 子流程，對照 OWASP Top 10:2025 + CWE + 機密管理逐類人工審查 + CI 既有 SAST/SCA。
> 範圍：backend/app 全模組。CI run #73 的 Semgrep(SAST) + pip-audit/npm audit(SCA) 皆 green。

**量化結論**：**Critical = 0、High = 0、Medium = 0**，Low/觀察 = 4（皆非漏洞，屬強化建議）。
資安 Exit Criteria（Critical/High = 0、無硬編碼密鑰、相依套件無 Critical CVE）→ **達標**。

**逐類審查結果（OWASP Top 10:2025）**：
- A01 權限控制失效：✅ 個人資源端點（notifications/daily-tasks/time-logs/reactions）全以 `user_id == current_user.id` 過濾；專案資源統一走 `require_project_membership` 中介層（deny-by-default）；`daily_tasks` 的 `target_user_id` 僅 admin 可用。無 IDOR、無越權。
- A02 安全設定：✅ CORS 來源走環境變數（預設 localhost 非 `*`）；無 debug/verbose 殘留。⚠️ 部署注意：勿把 `CORS_ORIGINS=*` 配 `allow_credentials=True`（部署責任，非程式碼漏洞）。
- A03 供應鏈：✅ requirements.txt 鎖版本；CI pip-audit + npm audit green。
- A04 加密失效：✅ 密碼 bcrypt 慢雜湊；LDAP/RADIUS 密鑰 Fernet 加密儲存；JWT HS256；無弱演算法、無自製加密。
- A05 注入（含 XSS/路徑遍歷）：✅ 全 ORM 參數化（零 f-string SQL）；無 os.system/eval/pickle；**附件儲存用 uuid 當檔名、只取使用者檔名 suffix → 防路徑遍歷**；輸入 schema 全有長度/pattern 驗證。
- A06 不安全設計：✅ 認證重構（G05）已修互斥鎖死；授權邊界明確。
- A07 認證失效：✅ bcrypt + login 限流（10/min）+ register 限流（5/min）。
- A08 完整性：✅ 唯一 json.loads 解析 Claude 自身回應（可信來源）+ try/except 回退；無不安全反序列化。
- A09 日誌監控：✅ 記錄登入失敗/權限事件，log 不含密碼/token 值。
- A10 例外處理：✅ 例外多 fail-safe 導向安全預設（crypto/auth/ldap 回退）。
- 機密管理：✅ 無硬編碼密鑰；預設弱密鑰由 `validate_production_secrets()` 在 production 擋下。

**Findings**：
- [x] FIND-S1 [Low][A07/CWE-307] `change-password` 無 rate limit → **已修**：加 `@limiter.limit("5/minute")`。
- [x] FIND-S2 [Low][A07/CWE-613] JWT 無版本，改密碼/登出後舊 token 仍有效 → **已修**：User 加 `token_version`（migration 018）；token 帶 `tv`；`get_current_user` 與 `refresh` 驗證 tv；改密碼時 `token_version += 1` 並回傳新 token。**殘餘**：WebSocket 連線不驗 tv（短連線、唯讀、風險低），列已知殘餘風險。
- [x] FIND-S3 [Low][A10/CWE-755] DB 查設定失敗的回退方向 → **已修**：register 改 fail-closed（查詢拋例外 → 503，不再預設放行）；`_get_auth_backend` 保留 local 回退（local 是安全選擇）但加 error log。
- [ ] FIND-S4 [Info][A05/LLM] ai_assist task title 進 Claude prompt：結果經 title_map 比對、僅影響自己排序、無跨使用者/外洩 → **接受為殘餘風險**，不修。

**驗證**：新增 test_security_hardening.py（改密碼失效舊 access/refresh token、新 token 可用、wrong-old 仍 400、register fail-closed 503）；全套 329 passed、覆蓋率 97%、三道 CI gate 本地皆綠。

---

### G07 — Ubuntu 24.04 一鍵部署 + HTTPS（自簽）+ 前端憑證管理（2026-06-10）

> CD 範疇。計畫：`swift-juggling-otter.md`（已核准）。架構：nginx(HTTPS) 唯一對外入口，DB/Redis/backend 內網不暴露。

**交付物**：
- [x] `frontend/nginx.conf`：HTTPS（443）+ HTTP→HTTPS 轉址 + SPA fallback + /api、/ws proxy + 安全標頭（nosniff/DENY/HSTS，A02）+ client_max_body_size 10m。
- [x] `docker-compose.prod.yml`：db/redis 不對外開 port；backend 無 --reload、APP_ENV=production、跑 migration + uvicorn --workers 2；nginx build production target；**reloader sidecar**（唯一掛 docker.sock，監看 certs 變更 → `docker kill -s HUP` 熱重載，backend 不碰 socket）。
- [x] `frontend/Dockerfile`：builder 接 VITE_API_URL/VITE_WS_URL build args（同源預設空）；production EXPOSE 80 443。
- [x] WS 同源：新增 `frontend/src/lib/ws.ts`，HTTPS 部署自動走 `wss://host`（兩個 WS hook 共用）。
- [x] 後端憑證 API：`app/core/tls_cert.py`（驗證 cert/key 相符+未過期、原子寫入、key 0600、固定檔名防遍歷）+ `system_settings.py` 的 `GET/POST /tls-cert`（admin only，私鑰不回傳/不入 log）。
- [x] 前端：SettingsPage「TLS 憑證」區（顯示現況 + 上傳 cert/key + 套用）。
- [x] `install.sh`：檢查 root/Ubuntu → 裝 Docker（冪等）→ 產 .env（openssl 強密鑰 + 互動密碼）→ 產自簽憑證注入 certs volume → up --build → 等 health → 印存取/告警/備份指引。

**驗證**：後端 338 passed、覆蓋率 97%；test_tls_cert.py 9 測試（上傳/驗證/key不符/過期/壞PEM/admin/0600 權限）；前端 build 過；install.sh shellcheck exit 0；prod compose config OK；frontend production image build + nginx 語法驗證（proxy upstream 在 compose 外解析不到屬預期）；三道 CI gate 本地全綠。

**安全（延續 G06）**：docker.sock 只給隔離 sidecar，backend/app 維持最小權限；憑證上傳 admin only + 嚴格驗證 + 固定檔名防遍歷；私鑰 0600、不回傳、不入 log；prod 不暴露 DB/Redis；APP_ENV=production 觸發強密鑰驗證。

**已知風險 / 待實機驗證**：
- install.sh 與完整 prod compose 起服務**需在目標 Ubuntu 24.04 實機驗證**（本開發機為 Windows，僅能靜態檢查 + 元件級驗證）。
- 自簽憑證會觸發瀏覽器告警（預期）；正式環境經前端上傳 CA 憑證即可。
- reloader sidecar 用 docker.sock 是 reload nginx 的必要設計，已縮到單一最小容器。

---

### G08 — Ubuntu 24.04 原生部署（非 Docker）（進行中，2026-06-10）

> CD 範疇的並行方案。使用者要求：**不用 Docker，直接原生部署在 Ubuntu 24.04 主機**。
> 與 G07 的 Docker 方案並存（兩套部署方式，使用者擇一）。架構：apt 原生服務 + systemd + nginx。
> CoreMain 對齊：服務「組織內部 100 人協作」上線需求，圍繞主題；遵守「簡單、不過度設計」。

**架構決策（使用者確認）**：
- **PostgreSQL**：apt `postgresql`，systemd 管理；建 workflow 使用者 + workflow_db。
- **Redis**：apt `redis-server`，systemd 管理（使用者要求保留，雖目前 codebase 未實際使用）。
- **後端**：Python venv + uvicorn，跑 systemd service（`--workers 2`，非 root 專屬使用者）。
- **前端**：`npm ci && npm run build`（tsc && vite build）產靜態檔，nginx serve。
- **入口**：原生 nginx 反向代理（HTTPS 443 + HTTP→HTTPS 轉址 + /api、/ws proxy + SPA fallback + 安全標頭），沿用 G07 的 nginx.conf 設計改成原生路徑。
- **憑證熱重載**：systemd **path unit** 監看憑證檔變更 → 觸發 `nginx -s reload`（取代 Docker 的 reloader sidecar；最小權限、後端不需提權）。
- **migration**：install 腳本內顯式 `alembic upgrade head`（原生不靠 lifespan）。

**上線標準（Exit Criteria 三維度）**：
- 功能：install-native.sh 一鍵在乾淨 Ubuntu 24.04 跑完；服務全 active；HTTPS 可連；admin 可登入；migration 至 head。
- 資安：服務跑在非 root 專屬使用者；.env 權限 600；私鑰 0600；強密鑰自動產生；APP_ENV=production 觸發強密鑰驗證；nginx 安全標頭；後端不對外（只聽 127.0.0.1）；憑證熱重載不給 backend 提權（教訓 64）。
- 品質：腳本冪等（重跑跳過已裝步驟）；bash -n + shellcheck（若可裝）通過；nginx -t 通過；unit 檔 systemd-analyze verify 通過；實機健康檢查通過。

#### 〔PM〕評估與設計（G1）
- [x] 與使用者確認：非 Docker、apt 原生服務、systemd+nginx、保留 Redis、path unit 熱重載、交付新腳本
- [x] 摸清專案結構：requirements.txt、config.py（env）、nginx.conf、alembic、前端 build、Redis 實際未用
- [ ] 寫入計畫與 Exit Criteria（本區塊）

#### 〔DevSecOps〕編寫與資安確認（G2/G3）
- [ ] `install-native.sh`：前置檢查 → apt 裝 postgres/redis/python3-venv/nodejs/nginx → 建系統使用者 → DB 初始化 → venv + pip install → alembic upgrade head → 前端 build → 產 .env（強密鑰）→ 自簽憑證 → 佈署 systemd units + nginx 設定 → enable/start → 健康檢查
- [ ] `deploy/native/workflow-backend.service`：uvicorn systemd unit（非 root、EnvironmentFile、systemd 強化指令）
- [ ] `deploy/native/workflow-cert-reload.path` + `.service`：監看憑證 → nginx reload
- [ ] `deploy/native/nginx-workflow.conf`：原生 nginx 設定（443 HTTPS + proxy 127.0.0.1:8000 + SPA + 安全標頭）
- [ ] 後端 TLS_CERT_DIR/UPLOAD_DIR 指向原生路徑（env 覆寫，免改 code）
- [ ] Sec：最小權限/密鑰/憑證權限/systemd 強化/無提權逐項確認（延續 G06/教訓 64）

#### 〔QA〕品質測試（G4/G5/G6）
- [ ] 靜態：bash -n、shellcheck（可裝則跑）、nginx -t、systemd-analyze verify
- [ ] 實機（使用者提供 SSH 的 Ubuntu 24.04）：跑全流程 → 服務 active → HTTPS+admin 登入 → 冪等重跑 → 健康檢查（對應 G6）

#### 關卡狀態
- [x] G1 PM→Dev：架構與 Exit Criteria 確認
- [x] G2 Dev→Sec：腳本 + 3 units + nginx 設定完成、bash -n + shellcheck 0 警告
- [x] G3 Sec→QA：資安 Exit Criteria 達標（非 root、127.0.0.1 內聽、0600、無提權、憑證時序、SQL/URL 注入防護）
- [x] G4 QA→結案：靜態驗證 + 實機驗證通過
- [x] G6 上線：**192.168.99.178 實機端到端達標**（4 服務 active、HTTPS health、HTTP→301、admin 登入 200、migration 18 版、SPA serve、冪等重跑一次過）

##### G6 實機抓到並修復的 6 個缺陷（靜態驗證過不了的）
1. psql `:'pw'` 在 `-c` 不展開 → 改 stdin 餵 SQL（教訓 69）
2. `read; echo` 換行污染 .env 密碼 → 提示寫 stderr + env 變數覆寫（教訓 70）
3. 前端 build 缺 devDependencies（tsc not found）→ `--include=dev`（教訓 71）
4. 無 package-lock.json → npm ci 失敗 → 偵測 lockfile 選 ci/install（教訓 71）
5. nginx 1.24 不支援 `http2 on;` → 改 `listen ssl http2`（教訓 72）
6. systemd ProtectHome 害 asyncpg stat $HOME SSL key 撞 PermissionError → `Environment=HOME=/opt/workflow`（教訓 73）

##### 待補（不阻斷上線）
- frontend 缺 `package-lock.json`：建議產生並提交，讓 build 可重現（目前 fallback npm install 可運作但版本不鎖定）。

---

## 已知風險

- N08 Claude API：需使用者提供 ANTHROPIC_API_KEY；CI 環境用 mock 測試
- 移除 F18/F22 的 DB table：評估是否保留 table（向下相容）或 migration down（清楚但不可逆）
- N07 健康指標改前端計算：需確認 tasks API 回傳足夠資訊
- F14 Emoji 反應 / F16 個人效率分析：暫緩但不刪除 code，留待未來需求
